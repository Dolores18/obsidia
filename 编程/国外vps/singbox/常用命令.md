---
创建时间: 星期六, 7月 6日 2024, 7:06 早上
最近修改: 
---
```
mkdir -p /home/sing-box && cd /home/sing-box && \
wget https://github.com/SagerNet/sing-box/releases/download/v1.10.0-alpha.19/sing-box-1.10.0-alpha.19-linux-amd64.tar.gz && \
tar -zxvf sing-box-1.10.0-alpha.19-linux-amd64.tar.gz && \
cd sing-box-1.10.0-alpha.19-linux-amd64 && \
chmod +x sing-box && \
sudo mv sing-box /usr/local/bin && \
sudo cat > /etc/systemd/system/sing-box.service << EOF
[Unit]
Description=sing-box service
Documentation=https://sing-box.sagernet.org
After=network.target nss-lookup.target

[Service]
CapabilityBoundingSet=CAP_NET_ADMIN CAP_NET_BIND_SERVICE
AmbientCapabilities=CAP_NET_ADMIN CAP_NET_BIND_SERVICE
ExecStart=/usr/local/bin/sing-box run -c /etc/sing-box/config.json
Restart=on-failure
RestartSec=10s
LimitNOFILE=infinity

[Install]
WantedBy=multi-user.target
EOF




```



查看日志
```
journalctl -u sing-box.service

sudo journalctl -u sing-box -n 50 --no-pager  #查看最新的日志
 

```
编辑sing-box 程序的配置
```
sudo vim /etc/systemd/system/sing-box.service
sudo vim /lib/systemd/system/sing-box.service
sudo systemctl daemon-reload
sudo systemctl restart sing-box



```