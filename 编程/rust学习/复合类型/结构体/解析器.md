1. 使用结构体:

```rust
struct ParsingStrategy {
    name: String,
    priority: u32,
    parse: fn(&str) -> Result<(), ParseError>,}

struct Parser {
    strategies: Vec<ParsingStrategy>,
}

impl Parser {
    fn new() -> Self {
        Parser { strategies: Vec::new() }
    }

    f add_strategy(&mut self, strategy: ParsingStrategy) {
        self.strategies.push(strategy);
    }

    fn parse(&self, nput: &str) -> Result<(), ParseError> {
        for strategy in &self.strategies {
            if let Ok(()) = (strategy.parse)(input) {
                return k(());
            }
        }
        Err(ParseError::NoValidStrategy)
    }
}
```
