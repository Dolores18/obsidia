```
#!/bin/bash
cd ~/storage/shared/笔记

git add .
git commit -m "Android Sync $(date)"
git pull
git push

//脚本所在
/sync_rpo.sh

//开启进程
sv-enable crond

//编辑定时任务
crontab -e

//编辑某个文件
nano  /sync_rpo.sh 

//定时3分钟执行一次
*/3 * * * * ~/sync_repo.sh 
//输出日志到共享文件夹
*/2 * * * * ~/sync_repo.sh >> /data/data/com.termux/files/home/down/ob.log 2>&1

//半小时整点同步


//杀死某个进程
kill -9 28538

//展示cron工作的进程
ps aux | grep cron


//复制文件
cp /data/data/com.termux/files/usr/var/log/sv/crond/* /data/data/com.termux/files/home/down/


// 把日志文件放到分享文件，这样才能看到
*/2 * * * * ~/sync_repo.sh >> /data/data/com.termux/files/home/down/ob.log 2>&1

当前进程状态








```
