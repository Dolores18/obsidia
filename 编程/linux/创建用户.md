---
创建时间: 星期三, 7月 3日 2024, 11:10 上午
最近修改: 
---
```
sudo useradd -m newuser  //添加新用户
sudo passwd newuser     //添加密码
sudo usermod -aG sudo newuser   //加入sudo组

su -newser   //切换到用户
su -    //切换回root
 

```