```dataview 
table file.ctime ,file.inlinks

where file.cday = date(today)
sort file.ctime asc
limit 100


```










```dataviewjs
dv.header(3, '今天创建的文件');
dv.list(
    dv.pages()
    .filter(p => moment(p.file.ctime.ts).isSame(moment(), 'day'))
    .map(p => `${p.file.name}: ${moment(p.file.ctime.ts).format('YYYY-MM-DD HH:mm:ss')}`)
);




```